import React, { Fragment, Dispatch } from "react";
import UserTable from './UserTable'

const Request = () => {
 

  return (
    <Fragment>
      <UserTable/>
    </Fragment>
  );
};

export default Request;
